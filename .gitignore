node_modules/
dist/
target/
src-tauri/target/
*.tsbuildinfo
.DS_Store
.idea/

# Per-machine Claude Code state (settings.local.json, worktrees, etc.)
.claude/

# Kodo orchestrator state (per-machine notes, run status, config)
.kodo/

# Tauri build artifacts: tauri-build regenerates these from
# src-tauri/capabilities/ on every cargo build.
src-tauri/gen/

# Benchmark LLM response cache (per-machine, populated by
# COACH_LLM_CACHE_DIR=benchmark/.cache cargo test ...)
benchmark/.cache/
